### Requirements
```
sudo apt-get update
sudo apt-get install nfdump
sudo apt-get install softflowd
sudo apt-get install iperf3
```

### Overview
Runs the NetFlow V9 inside Mininet for the given topology.

\
Will be Updated soon.

### Instructions 
```
sudo netflow.py 
```
Then follow the outout logs.